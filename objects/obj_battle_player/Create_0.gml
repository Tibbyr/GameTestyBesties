// This is weird. The reason this works is because it's looking at the instance variable which only exists at runtime, this logic is defined in obj_player collision object

data = obj_battle_switcher.player_data;
